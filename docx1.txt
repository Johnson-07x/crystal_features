from pymatgen.io.cif import CifParser
from io import StringIO
from urllib.request import urlopen

request = urlopen("https://raw.githubusercontent.com/sheriftawfikabbas/crystalfeatures/master/Li10Ge(PS6)2_mp-696128_computed.cif")
cifFile = request.read().decode('utf-8')

# Use StringIO to create a file-like object from the string
cifStringIO = StringIO(cifFile)

# Create a CifParser instance using the file-like object
parser = CifParser(cifStringIO)

# Get the structure object from the parser
structure = parser.get_structures()[0]

# Now you can work with the structure object, such as calculating the band gap


----------------------------------------------------------------------------------

